import '../../scss/conponents/goods.scss'

import React from 'react';

interface Props {}
const Component: React.FC<Props> = (props) => {

	return (
		<div className='module_goods'>
            至商品页配置
		</div>
	)
}

export default React.memo(Component);